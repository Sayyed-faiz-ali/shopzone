<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="flex flex-wrap justify-center gap-4 mt-6 p-6">
        <% x.forEach(function(i) { %>
            <div class="w-full sm:w-1/3 md:w-1/4 lg:w-1/5">
        <img class="w-full h-32 object-cover rounded-t-lg" style="float: left;padding: 2px;margin: 10px; border: 2px solid cadetblue ;border-radius:10% 10% 10%;" src="data:image/jpeg;base64,<%= i.buffer.toString('base64') %>" alt="<%= i.originalname %>">

            </div>
        <% }); %>
    </div>
    
    
</body>
</html>